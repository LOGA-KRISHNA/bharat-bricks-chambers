import { Box, Layers3, ScanLine } from "lucide-react";
import { SectionHeading } from "@/components/ui/SectionHeading";

const steps = [{ icon: Layers3, name: "Study the surface", text: "Start with intended appearance, context and system constraints." }, { icon: ScanLine, name: "Review the detail", text: "Align samples, joints and installation approach with the design team." }, { icon: Box, name: "Build with clarity", text: "Keep verified documentation and project decisions close to the work." }];
export function Technology() { return <section className="bg-ink px-5 py-20 text-paper md:px-8 md:py-32"><div className="mx-auto max-w-[1280px]"><SectionHeading eyebrow="Project intelligence" title="Better material decisions, earlier." text="A clear process makes room for architectural intent while supporting sound project coordination." /><div className="mt-14 grid gap-px bg-paper/20 md:grid-cols-3">{steps.map((step, index) => { const Icon = step.icon; return <article className="bg-ink p-7" key={step.name}><span className="eyebrow text-[#d9b17a]">0{index + 1}</span><Icon className="mt-12" size={29} strokeWidth={1.25} /><h3 className="mt-6 text-xl">{step.name}</h3><p className="mt-3 max-w-xs text-sm leading-6 text-paper/65">{step.text}</p></article>; })}</div></div></section>; }
