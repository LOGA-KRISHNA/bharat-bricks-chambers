import { Leaf, Recycle, Timer } from "lucide-react";
import { SectionHeading } from "@/components/ui/SectionHeading";

const principles = [{ icon: Timer, title: "Designed for longevity", text: "The most responsible material choice starts with a facade that is carefully specified for its intended life." }, { icon: Recycle, title: "Consider the whole assembly", text: "Review system, maintenance and end-of-life needs with qualified project professionals." }, { icon: Leaf, title: "Ask for evidence", text: "Environmental claims and material data should be confirmed against current, verified product documentation." }];
export function Sustainability() { return <section className="bg-[#c8b69e] px-5 py-20 md:px-8 md:py-32"><div className="mx-auto max-w-[1280px]"><div className="grid gap-14 lg:grid-cols-[.9fr_1.1fr]"><SectionHeading eyebrow="Material responsibility" title="A considered approach lasts longer." text="We believe material conversations should be precise, transparent and project-specific." /><div className="grid gap-8">{principles.map((item) => { const Icon = item.icon; return <article className="grid grid-cols-[42px_1fr] gap-4 border-t border-ink/20 pt-5" key={item.title}><Icon size={25} strokeWidth={1.3} /><div><h3 className="text-lg">{item.title}</h3><p className="mt-2 max-w-md text-sm leading-6 text-ink/70">{item.text}</p></div></article>; })}</div></div></div></section>; }
