import Link from "next/link";
import { ArrowUpRight } from "lucide-react";
import { projects } from "@/data/projects";
import { SectionHeading } from "@/components/ui/SectionHeading";

const colors: Record<string, string> = { terracotta: "#9a452e", sand: "#d8c19c", charcoal: "#2d2a28", coral: "#c45d42", olive: "#7b7862", stone: "#a59a8b" };
export function Gallery({ preview = false }: { preview?: boolean }) { const items = preview ? projects.slice(0, 4) : projects; return <section className="px-5 py-20 md:px-8 md:py-32"><div className="mx-auto max-w-[1280px]"><div className="flex items-end justify-between gap-4"><SectionHeading eyebrow="Visual journal" title="Material in context." /><Link href="/gallery" className="hidden items-center gap-2 text-sm underline underline-offset-8 sm:flex">View all <ArrowUpRight size={16} /></Link></div><div className="mt-12 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{items.map((project, index) => <article className={`group relative min-h-68 overflow-hidden p-5 text-paper ${index === 0 && preview ? "lg:col-span-2" : ""}`} style={{ backgroundColor: colors[project.tone] }} key={project.title}><div className="absolute inset-0 opacity-35" style={{ backgroundImage: "repeating-linear-gradient(0deg, transparent 0 34px, rgba(20,10,7,.8) 35px 37px), repeating-linear-gradient(90deg, transparent 0 86px, rgba(20,10,7,.55) 87px 89px)" }} /><div className="relative flex h-full min-h-58 flex-col justify-between"><span className="eyebrow text-paper/70">{project.category}</span><div className="flex items-end justify-between"><h3 className="display max-w-xs text-3xl leading-none">{project.title}</h3><ArrowUpRight className="opacity-0 transition-opacity group-hover:opacity-100" /></div></div></article>)}</div></div></section>; }
